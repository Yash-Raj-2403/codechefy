#include <bits/stdc++.h>
using namespace std;

int main() {
    // your code goes here
    int t;
    cin >> t;
    while(t--)
    {
        int n,k;
        cin >> n >> k;
        vector<int> a(n);
        for(int i=0;i<n;i++)
        {
            cin >> a[i];
        }
        sort(a.begin(),a.end(),greater<int>());
        long pr=0;
        for(int i=0;i<k;i++)
        {
            if(a[i]>=5)
            {
                pr += (a[i]-5);
            }
        }
        for(int i=k;i<n;i++)
        {
            if(a[i]>=10)
            {
                pr+=(a[i]-10);