using namespace std;

int main() {
    // your code goes here
    int t;
    cin >> t;
    while(t--)
    {
        int n;
        cin >> n;
        vector<int> a(n);
        for(int i=0;i<n;i++)
        {
            cin >> a[i];
        }
        int c=0;
        int maxa = *(max_element(a.begin(),a.end()));
        int mina = *(min_element(a.begin(),a.end()));
        for(int i=0;i<n;i++)
        {
            if(a[i]!=mina && a[i]!=maxa)
            {
                c++;
            }
        }
        cout << c << endl;
    }
}
